/**
 * Exports the `UserProfile` and `UserProfileContainer` components from the `./UserProfile` and `./UserProfileContainer` files, respectively.
 */
// src/components/UserProfile/index.js
export { default as UserProfile } from "./UserProfile";
export { default as UserProfileContainer } from "./UserProfileContainer";
