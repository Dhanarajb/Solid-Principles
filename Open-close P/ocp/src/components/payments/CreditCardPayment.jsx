/**
 * Processes a credit card payment for the given amount.
 *
 * @param {number} amount - The amount to be charged to the credit card.
 * @returns {void}
 */
export const processCreditCardPayment = (amount) => {
    console.log(`Processing credit card payment of $${amount}`);

};
